{
  "task_id": "task_024_concat_where",
  "source_library": "pandas",
  "target_library": "polars",
  "description": "Migrate pd.concat(axis=1) for horizontal combination of DataFrames, Series.where(condition, other) for value clamping (semantics: keep where True, replace where False), and pd.concat(axis=0) for vertical stacking — from pandas to Polars.",
  "expected_changed_files": [
    "src/pipeline/combine.py",
    "requirements.txt"
  ]
}
